{
  "name": "cert-verifier",
  "private": true,
  "version": "1.0.0",
  "description": "University of Swabi certificate verifier dApp backend",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17"
  },
  "dependencies": {
    "ethers": "^6.13.4"
  }
}
